# Local Hardhat JSON-RPC endpoint.
RPC_URL=http://127.0.0.1:8545

# Use a funded account printed by `npm run node`.
# Never use a production wallet or a key holding real funds.
EVIDENCE_SIGNER_PRIVATE_KEY=
